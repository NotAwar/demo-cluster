---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kagent
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: kmcp
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: kmcp-repo
        namespace: flux-system